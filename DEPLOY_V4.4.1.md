# v4.4.1 — Railway Config Fix

Corrige `railway.toml` y añade una prueba que verifica que el archivo sea TOML válido,
sin BOM, con el comando de inicio, política de reinicio y cron esperados.

No modifica collectors, alertas, base de datos ni la optimización paralela de v4.4.
