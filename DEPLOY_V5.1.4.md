# Deploy v5.1.4

Corrige dos fallos observados en la primera ejecución de v5.1.3:

- `PhaseMetrics.merge()` inexistente.
- URLs Shopify con formato `/collections/<coleccion>/products/<slug>` no reconocidas por El Mundo del Vino.

No agrega migraciones ni variables.
